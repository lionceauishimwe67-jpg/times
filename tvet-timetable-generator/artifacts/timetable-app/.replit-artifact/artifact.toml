kind = "web"
previewPath = "/"
title = "TVET Timetable Generator"
version = "1.0.0"
id = "artifacts/timetable-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23512

[services.development]
run = "pnpm --filter @workspace/timetable-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/timetable-app", "run", "build" ]
publicDir = "artifacts/timetable-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23512"
BASE_PATH = "/"
